/**
 * @param {character[][]} board
 * @return {boolean}
 */
var isValidSudoku = function(board) {

  var yl = board.length, xl = board[0].length;

  if (yl < 0 || xl < 0) {
    return false;
  }

  var x, y;

  // check each row
  for (y = 0 ; y < yl ; y++) {

    for (x = 0 ; x < xl ; x++) {

      

    }

  }

};